/* Project specific Javascript goes here. */
function fetchTerrain() {
    let endpoint = '/api/terrain/';
    let host = window.location.protocol + '//' + window.location.hostname;
    console.log(host + endpoint);
}

function showTerrain() {

}